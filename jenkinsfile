@Library('todo') _

todo (
        COMPONENT             : 'login',
        PROJECT_NAME          : "todo",
        SLAVE_LABEL           : "agent",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)
